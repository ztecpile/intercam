import { InstructionsDirective } from './instructions.directive';

describe('InstructionsDirective', () => {
  it('should create an instance', () => {
    const directive = new InstructionsDirective();
    expect(directive).toBeTruthy();
  });
});
