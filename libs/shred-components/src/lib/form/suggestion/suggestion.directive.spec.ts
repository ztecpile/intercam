import { SuggestionDirective } from './suggestion.directive';

describe('SuggestionDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestionDirective();
    expect(directive).toBeTruthy();
  });
});
